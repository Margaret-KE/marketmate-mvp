class Product:
    def __init__(self, id, name, price):
        self.id = id
        self.name = name
        self.price = price

    # Add methods for product-related operations (e.g., saving to database, retrieving from database)

